'use strict';

const List = require('./list-constructor.js');

// let last = stuff.pop();

let stuff = new List();
stuff.push('a');
stuff.push('b');
stuff.push('c');
stuff.push('d');
// stuff.push('e');
// stuff.push('f');
// stuff.push('g');

console.log({stuff});
// stuff.shift();
// console.log({stuff});

// stuff.unshift(['x', 'y', 'z']);
// console.log({stuff});
// console.log(stuff.length);
// stuff.splice(2,2,'four','seven','eight','two');

// console.log({stuff});

// console.log(stuff.slice(2,8));
